name: rubbish_calc
description: Check how much rubbish you produce every day.
version: 1.0.0-dev
author: mattmaniak
repository: https://gitlab.com/mattmaniak/rubbish_calc.git

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cloud_firestore: ^0.13.5
  email_validator: ^1.0.4
  flutter_secure_storage: ^3.3.3
  firebase_auth: ^0.15.5+3
  firebase_core: ^0.4.4+3
  url_launcher: ^5.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1
  test: any

flutter:
  uses-material-design: true
